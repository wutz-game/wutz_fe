<h1 align="center">Wutz</h1>
<div class="game-grid">
  <table class="table-container">
    <thead>
      <tr>
        <% @categories.each do |category| %>
          <th class="header"><%= category %></th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% @questions.each_with_index do |row, row_idx| %>
        <tr>
          <% row.each_with_index do |question, col_idx| %>
            <td class="game-cell" id="cell_<%= row_idx %>_<%= col_idx %>" data-question="<%= question %>">
              <%= question %>
            </td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div id="popup" style="display:none;">
  <span id="closeBtn" style="cursor:pointer;">X</span>
  <h2>Question</h2>
  <p id="question"></p>
  <form id="myForm">
    <input type="text" id="myInput" placeholder="Enter text...">
    <button type="submit">Submit</button>
  </form>
  <div id="result"></div>
</div>
